-- Gera/usa um app (via osacompile) que abre o Terminal e inicia o bot.
-- O build script substitui __PROJECT_DIR__ pelo caminho absoluto do projeto.

set projectDir to "__PROJECT_DIR__"
set runCmd to "cd " & quoted form of projectDir & " && npm start"

tell application "Terminal"
	activate
	do script runCmd
end tell
